<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="8dp"
    android:background="@drawable/item_background">

    <TextView
        android:id="@+id/bonded_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:textSize="18sp"
        tools:text="Name"/>

    <TextView
        android:id="@+id/bonded_item_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bonded_item_name"
        android:layout_marginTop="8dp"
        android:textSize="18sp"
        tools:text="Address"
        />

    <TextView
        android:id="@+id/bonded_item_class"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bonded_item_address"
        android:layout_marginTop="8dp"
        android:textSize="18sp"
        tools:text="Class"
        />

    <TextView
        android:id="@+id/bonded_item_uuids"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/bonded_item_class"
        android:layout_marginTop="8dp"
        android:textSize="18sp"
        tools:text="Class"
        />

</RelativeLayout>